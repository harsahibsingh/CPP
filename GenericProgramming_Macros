#include<iostream>

#define Max(a,b) (a > b ? a:b)

int main()
{
    int x=5,y=3;
    
    float a= 10.15, b=15.76;
    
    std::cout<<"Max Int: "<< Max(x,y)<<std::endl;
    
    std::cout<<"Max Float: "<<Max(a,b)<<std::endl;
    
    return 0;
}
